# Mohdanas
cd /path/to/your/site-folder   # folder that contains index.html
git init
git add .
git commit -m "Initial site"
git branch -M main
git remote add origin https://github.com/USERNAME/my-site.git
git push -u origin main
